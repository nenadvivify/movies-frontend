import React from 'react';
import './style.scss';

const Footer = ({ className }) => {
    return (
        <footer>
        	<div className="title">Pocket IMDB</div>
        	<i>Created by Nenad Vracar</i>
        </footer>
    );
};

export default Footer;
